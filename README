
полувложение(тип)
    текст внутри текста

вложение(тип)

полупроцедура
    struct полупроцедура {
        ???
    }

процедура
    полупроцедуры и терминатор

    struct процедура {
        ListNew ( struct полупроцедура )
        struct терминатор b;
    }

подпроцедура
    оператор счётная полупроцедура терминатор

счётная полупроцедура
    операторы и слова

слово

оператор

процедура счётности
    слово оператор счётная полупроцедура терминатор

языковая философия ====================================

<процедура> [счётности]
    слово оператор [счётная] полу<процедура> терминатор

[хвост] <процедуры>
    слово оператор терминатор

<процедура> [счётности]
    полу(<процедура> [счётности]) [хвост] <процедуры>

Ф
    полу Ф [хвостик] <процедуры>
    функция-коструктор процедуры Ф при полу-хвостике